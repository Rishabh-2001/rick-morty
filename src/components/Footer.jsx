import React from 'react'
import headerStyle from "../style/Header.module.css";
const Footer = () => {
  return (
    <div className={`${headerStyle?.headerContainer} ${headerStyle?.boxRow}`}>
        <h2 style={{color:""}}>Footer</h2>

    </div>
  )
}

export default Footer